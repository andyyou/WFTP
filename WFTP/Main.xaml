<Window x:Class="WFTP.Main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="查詢" Height="600" Width="800" WindowStyle="None" ResizeMode="NoResize">
    <Grid
        x:Name="LayoutRoot"
        Background="White">
        <Grid 
            x:Name="HeaderGrid" 
            Height="50" 
            VerticalAlignment="Top">
            <Grid
                x:Name="DragableArea"
                Background="White"
                MouseDown="DragableGridMouseDown"/>
            <StackPanel
                Orientation="Horizontal"
                Margin="0,5,5,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="White">
                <TextBlock 
                    x:Name="ChangeViewButton"
                    Text="2"
                    FontFamily="Webdings"
                    Foreground="Gray"
                    Margin="0"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Right"
                    MouseLeftButtonUp="ChangeViewButtonMouseLeftButtonUp" />
                <TextBlock
                    x:Name="MinimizeButton"
                    Text="0"
                    FontFamily="Webdings"
                    Foreground="Gray"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    MouseLeftButtonUp="MinimizeButtonMouseLeftButtonUp" />
                <TextBlock
                    x:Name="MaximizeButton"
                    Text="1"
                    FontFamily="Webdings"
                    Foreground="Gray"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    MouseLeftButtonUp="MaximizeButtonMouseLeftButtonUp" />
                <TextBlock
                    x:Name="CloseButton"
                    Text="r"
                    FontFamily="Webdings"
                    Foreground="Gray"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    MouseLeftButtonUp="CloseButtonMouseLeftButtonUp" />
            </StackPanel>
        </Grid>
        <Button Content="查詢資料" Height="23" HorizontalAlignment="Left" Margin="20,12,0,0" Name="btnQuery" VerticalAlignment="Top" Width="75" />
        <Button Content="管理資料" Height="23" HorizontalAlignment="Left" Margin="112,12,0,0" Name="btnManage" VerticalAlignment="Top" Width="75" />
        <Button Content="上傳資料" Height="23" HorizontalAlignment="Left" Margin="204,12,0,0" Name="btnUpload" VerticalAlignment="Top" Width="75" />
        <Button Content="進階查詢" Height="23" HorizontalAlignment="Left" Margin="295,12,0,0" Name="btnAdvanceQuery" VerticalAlignment="Top" Width="75" />
    </Grid>
</Window>
